{
  "name": "visitcare-firestore-rules",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.9.0",
    "vitest": "^3.0.5"
  }
}
