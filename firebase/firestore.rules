rules_version = '2';

// TODO: Phase 3+ で本番用ルールに置き換え（Firebase Auth連携）
// 開発・デモ用: 全アクセス許可
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
